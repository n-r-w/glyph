version: "3"

# go-task Taskfile
# https://taskfile.dev

dotenv: [".env"]
silent: true

tasks:
  default:
    desc: "List available tasks"
    cmds:
      - task --list

  lint:
    desc: "Run code linters and vulnerability checks"
    cmds:
      - go tool nilness ./...
      - go tool easyp lint
      - go tool golangci-lint run --config ./.golangci.yml --build-tags=integration
      - go tool ifaceguard --config ifaceguard.cfg
      - go tool govulncheck ./...
      - task: lint-external-plugins

  lint-external-plugins:
    internal: true
    dir: testdata/external-plugins
    cmds:
      - $(cd ../.. && go tool -n golangci-lint) run --config ../../.golangci.yml --build-tags=integration ./...

  test:
    desc: "Run unit tests"
    cmds:
      - go test -race ./...
      - task: test-external-plugins

  test-external-plugins:
    internal: true
    dir: testdata/external-plugins
    cmds:
      - go test -race ./...

  itest:
    desc: "Run integration tests"
    cmds:
      # Integration tests start real plugin, terminal, and shell subprocesses. Run packages and tests serially.
      - go test -race -tags=integration -p 1 -parallel 1 ./...
      - task: itest-external-plugins

  itest-external-plugins:
    internal: true
    dir: testdata/external-plugins
    cmds:
      - go test -race -tags=integration -p 1 -parallel 1 .

  fix:
    desc: "Run go fix the codebase"
    cmds:
      - go fix ./...
      - task: fix-external-plugins

  fix-external-plugins:
    internal: true
    dir: testdata/external-plugins
    cmds:
      - go fix ./...

  fix_dry_run:
    desc: "Show go fix changes without modifying files"
    cmds:
      - go fix -diff ./...
      - task: fix-dry-run-external-plugins

  fix-dry-run-external-plugins:
    internal: true
    dir: testdata/external-plugins
    cmds:
      - go fix -diff ./...

  fmt:
    desc: "Format the codebase"
    cmds:
      - go fmt ./...
      - go tool golangci-lint fmt --config .golangci.yml ./...
      - task: fmt-external-plugins

  fmt-external-plugins:
    internal: true
    dir: testdata/external-plugins
    cmds:
      - go fmt ./...
      - $(cd ../.. && go tool -n golangci-lint) fmt --config ../../.golangci.yml ./...

  vet:
    desc: "Run go vet"
    cmds:
      - go vet ./...

  generate:
    desc: Generate code
    cmds:
      - go tool easyp generate
      - go generate ./...

  build:
    desc: "Build the Host and standard plugins"
    cmds:
      - mkdir -p {{default "bin" .BUILD_ROOT}}/plugins/extension {{default "bin" .BUILD_ROOT}}/plugins/ui
      - go build -o {{default "bin" .BUILD_ROOT}}/glyph ./host/cmd/glyph
      - go build -o {{default "bin" .BUILD_ROOT}}/plugins/extension/glyph-tools ./plugins/extension/tools/cmd/glyph-tools
      - go build -o {{default "bin" .BUILD_ROOT}}/plugins/ui/glyph-tui ./plugins/ui/tui/cmd/glyph-tui

  test-coverage:
    desc: Check combined test coverage
    vars:
      COVERAGE_MIN: '{{default "80" .COVERAGE_MIN}}'
    cmds:
      - |
        set -euo pipefail

        tmp_dir="$(mktemp -d)"
        trap 'rm -rf "$tmp_dir"' EXIT

        packages="$(go list ./... | grep -v '/experiments/')"
        cover_packages="$(printf '%s\n' "$packages" | paste -sd, -)"

        if ! go test -count=1 -coverpkg="$cover_packages" $packages \
          -args -test.gocoverdir="$tmp_dir" >"$tmp_dir/unit.log" 2>&1; then
          cat "$tmp_dir/unit.log" >&2
          exit 1
        fi

        if ! go test -count=1 -tags=integration -p 1 -parallel 1 \
          -coverpkg="$cover_packages" $packages \
          -args -test.gocoverdir="$tmp_dir" >"$tmp_dir/integration.log" 2>&1; then
          cat "$tmp_dir/integration.log" >&2
          exit 1
        fi

        go tool covdata textfmt -i="$tmp_dir" -o="$tmp_dir/coverage.out"

        awk 'NR == 1 || $0 !~ /(\.pb|_mock)\.go:/' "$tmp_dir/coverage.out" |
          go tool cover -func=/dev/stdin |
          awk -v minimum="{{.COVERAGE_MIN}}" '
            /^total:/ {
              gsub("%", "", $3)
              printf "Coverage: %.1f%%, required: %.1f%%\n", $3, minimum
              exit ($3 + 0 < minimum + 0)
            }
          '
