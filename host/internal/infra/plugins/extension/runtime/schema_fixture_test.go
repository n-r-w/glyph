package runtime

// validSchemaJSON is the shared valid tool schema fixture for unit and integration tests.
const validSchemaJSON = `{"type":"object","properties":{"path":{"type":"string","description":"File path."}},"required":["path"],"additionalProperties":false}`
